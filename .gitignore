# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
# alternate dist dir used by the preview dev server (.claude/launch.json)
.next-preview/
out/

# env — keep .env.local.example, never commit .env.local
.env.local
.env.*.local

# audit/prune output — contains customer ids, never commit it
revenuecat-*.csv

# misc
.DS_Store
*.pem
npm-debug.log*
firestore-debug.log
tsconfig.tsbuildinfo

# local PDF export dumps — never commit (5MB+ binaries)
export-check.pdf
scratch-export.pdf
*-export.pdf
.vercel
.env*
